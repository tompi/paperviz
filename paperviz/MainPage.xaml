<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:paperviz="clr-namespace:paperviz;assembly=paperviz"
             x:DataType="paperviz:MainPageViewModel"
             BackgroundColor="White"
             x:Class="paperviz.MainPage">

    <StackLayout>
        <Frame BackgroundColor="#235434" Padding="24" CornerRadius="0">
            <Label Text="PaperViz" HorizontalTextAlignment="Center" TextColor="White" FontSize="36"/>
        </Frame>
        <Button Text="Scan"
                Padding="20"
                Margin="20"
                FontSize="Title"
                Command="{Binding ScanCommand}"
                HorizontalOptions="Center"/>
        <Image Source="{Binding PhotoPath}"></Image>
        <ScrollView>
            <Label Text="{Binding Text}" TextColor="Black"></Label>
        </ScrollView>
    </StackLayout>
</ContentPage>
